// Tailwind CSS v4 is configured CSS-first (see `app/globals.css`, which uses
// `@import "tailwindcss"`). Design tokens live as CSS variables in globals.css,
// so no JS/TS config is required. This file is intentionally a no-op and is not
// referenced via `@config`.
const config = {};
export default config;

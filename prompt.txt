Role:
You are a senior staff engineer focused on production reliability + product manager who wants best UX.

Goal:
Make this expense-tracker PWA production-grade, bug-free, and native-iOS-like while preserving:
- Core flows (auth, expenses CRUD, accounts, analytics filters, CSV export, theme toggle, bottom nav)
- Existing visual identity (orange accent + current light/dark theme language)

Must-fix priorities:
1) Build stability
- Remove build-time network dependency from `next/font/google` Inter fetch failure.

2) iOS/PWA quality
- Fix safe-area behavior for header/content/bottom nav/modal/toasts.
- Ensure installed PWA mode works correctly and API data is never stale.

3) State/data integrity
- Eliminate duplicate/racy bootstrap fetches.
- Make loading/error states deterministic and user-visible.

4) Modal/input UX
- Refactor Add Expense modal gestures to robust typed pointer/touch handling (no `any` hacks).
- Ensure keyboard + numpad behavior is reliable on iPhone.

6) Improve the Code quality

7) iOS-native polish (without redesign)
- Improve small-text readability, touch targets, spacing rhythm, contrast, motion subtlety.
- Replace hover-dependent patterns with touch-first interactions.

Non-negotiables:
- No feature regressions.
- No schema-breaking DB changes unless migration is included.
- Keep TypeScript strict and code maintainable.
- make the PWA production grade with premium touch It should feel like a native ios app not a random PWA and also make it responsive especially for iphone16pro




